#include "stdafx.h"
#include "UI.h"


UI::UI()
{
	p1Bar = sf::RectangleShape(sf::Vector2f(healthWidth, healthHeight));
	p2Bar = sf::RectangleShape(sf::Vector2f(healthWidth, healthHeight));
}

void UI::update()
{

}

